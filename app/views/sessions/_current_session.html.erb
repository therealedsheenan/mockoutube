<nav class="flex items-center justify-between flex-wrap bg-teal-500 p-6">
  <div class="w-full block flex-grow lg:flex lg:items-center lg:w-auto">
    <% if current_user %>
      <div class="text-sm lg:flex-grow">
        <h3>Welcome, <strong><%= current_user.username %></strong>.</h3>
      </div>
      <%= link_to "Log Out", logout_path, :class => "block mt-4 lg:inline-block lg:mt-0 text-red-200 hover:text-white mr-4" %>
    <% else %>
      <%= link_to "Sign Up", signup_path, :class => "block mt-4 lg:inline-block lg:mt-0 text-teal-200 hover:text-white mr-4" %>
      <%= link_to "Log In", login_path, :class => "block mt-4 lg:inline-block lg:mt-0 text-teal-200 hover:text-white mr-4" %>
    <% end %>
  </div>
</nav>
